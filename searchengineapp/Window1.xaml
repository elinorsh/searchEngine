<Window x:Class="SearchEngineApp.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SearchEngineApp"
        mc:Ignorable="d"
        Title="Window1" Height="300" WindowState="Maximized" Width="300" Name="window" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
        <TextBlock Name="text" Height="{Binding ElementName=window}" Width="{Binding ElementName=window}" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto"></TextBlock>
        </ScrollViewer>
    </Grid>
</Window>
